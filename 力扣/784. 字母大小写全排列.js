/**
 * @param {string} s
 * @return {string[]}
 */
 var letterCasePermutation = function(s) {
   const len = s.length;
    let list = [];
   function turnChar(str,index){
    if(index === len){
      return
    }
    
   }
};

const s = 'a1b2';

console.log(letterCasePermutation(s));
